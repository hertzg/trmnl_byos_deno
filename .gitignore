.env
.env.*
!.env.example
.idea/
.vscode/
*.log
.DS_Store
.claude/

# Live config tree (ADR-0010, ADR-0012): all mutable operator content lives under
# config/live/. The committed *.example.ts starters and config/deno.json are
# image-owned; only config/live/ is the bind-mount boundary.
config/live/
