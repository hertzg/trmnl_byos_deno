// Starter template for the gitignored `album.ts`. Copy this file and paste in
// your own album link:
//
//   cp config/plugins/gallery/album.example.ts config/live/plugins/gallery/album.ts
//
// The link is the album's "Public Website" URL exactly as an Apple device
// shares it (enable via the album's settings; no credentials are involved).
// The Gallery discerns the album token from the URL fragment. With the
// placeholder left in place the Gallery renders its empty state with a note
// explaining the problem until a real link is saved (webproc restarts the
// server on save).
export const ALBUM_URL: string = "https://www.icloud.com/sharedalbum/#PASTE-TOKEN";
